import { expect } from 'chai';

describe('parsePptx', () => {
  describe('Given the sample .pptx file', () => {
    it('should be able to read the file', () => {
      expect(1).to.eq(1);
    });
  });
});
